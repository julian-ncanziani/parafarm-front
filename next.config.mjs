/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tailwindui.com'], // Asegúrate de incluir los dominios que necesites
      },
};

export default nextConfig;
